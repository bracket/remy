# remy config

The remy config file is a Python script which is loaded by remy at
runtime before loading and parsing notecard files.  It should be stored at the
base of a notecard repository and named `.remy_config.py`.  A sample one is located in `docs/examples/remy_config.py`.
